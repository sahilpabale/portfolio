# sahil.bio

portfolio and writing. v2 — minimal, light, serif/sans.

built with [astro](https://astro.build). fonts: inter + eb garamond.

```sh
pnpm install
pnpm dev      # local dev
pnpm build    # static build to dist/
```

posts live in `src/content/writing/` as mdx files.
